services:
  CLIProxyAPIManager:
    image: ghcr.io/a389216533/cliproxyapimanager:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/data
